# upload_config.py.example
# -------------------------
# Copy this file to "upload_config.py" (same folder) and fill in the real
# values below. upload_config.py is gitignored on purpose — it holds a
# secret (API_KEY) and must never be committed. This example file is the
# only one tracked in the repo.

SERVER_URL = "https://your-app-name.up.railway.app"
API_KEY = "paste-the-same-long-random-string-you-set-in-railway"
